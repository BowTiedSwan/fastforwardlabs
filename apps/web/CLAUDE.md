@AGENTS.md

## Claude Code notes

- Run `pnpm test` and `pnpm lint` (from repo root) as the fast ground-truth checks; full `pnpm build` only when touching routing/config.
- Keep responses tight: file paths with line refs, no pasted file dumps when a path + command reproduces the finding.
